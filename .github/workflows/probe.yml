name: probe

# Temporary. TASK-0269 / RES-0162 finding 1: does a public repository run
# Actions while the account payment gate blocks the private repo? Delete
# once the answer is recorded.
on:
  push:
  workflow_dispatch:

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - run: echo ok
