# Pinned for a reproducible `mkdocs build --strict`.
# mkdocs-static-i18n's optional `material` extra caps Material at <9.7.2;
# pin below that even when installing the two packages separately.
mkdocs-material==9.6.21
mkdocs-static-i18n==1.3.0
